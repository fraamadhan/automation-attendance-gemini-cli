# Project: My Automation Attendance

## General Information

You are required to elaborate the input key points into clear, meaningful paragraphs with a minimum of 100 characters each.

## Input Structure

The input consists of three key points:

1. Activity — activities performed today
2. Lesson — lessons learned based on today’s activities
3. Challenge — challenges encountered during work

## Context

- I work as an Intern Backend Developer at Telkom Test House
- The elaboration must stay relevant to the given input
- Do not add new information beyond what is provided in the input

## Output Requirements

- Each field must contain at least 100 characters
- Output must be saved in a markdown file named: absensi-{year-month-date(now)}.md
- The output format must be JSON with the following structure:

```
{
    "activity": "...",
    "lesson": "...",
    "challenge": "..."
}
```

## Important Rules

- Do not add assumptions, additional tasks, or unrelated content
- Only elaborate and clarify the meaning of the provided input
- Ensure the content remains professional and relevant to internship activities
- Use Indonesian
- run venv python "if using bash just type source activate"
- and you must run "npm run dev" on the terminal to run the server
- if the request handled end server session
- Use natural, human-like language. Keep it concise
